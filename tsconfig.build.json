{
  "//": "Build-only TS project for `next build` type-checking (Blocker-E). Extends the base config but excludes non-shipping code (unit tests + e2e/playwright), so the production build type-checks shipping code only. The editor and any test runner keep using tsconfig.json (which still includes tests).",
  "extends": "./tsconfig.json",
  "exclude": [
    "node_modules",
    "**/*.test.ts",
    "**/*.test.tsx",
    "**/*.spec.ts",
    "**/*.spec.tsx",
    "e2e",
    "playwright.config.ts"
  ],
  "compilerOptions": {
    "jsx": "preserve"
  }
}
