# Trust recipe (glass)

**Know → Verify (private) → Trust (mutual).**  
Verify is collaboration in the real world or another channel. The app only stores *your* mark.

## Onboarding doors
**Start** (new card) · **Continue** (open a vault). Join/Know stays easy. Trust is not a badge.

## Constitution (reach) — Hypatia to freeze
See `src/lib/trust/trust-recipe.ts` `TRUST_RECIPE_COPY`.

PSI / `visible()` still fleet. Glass does not invent know-layer or trust-layer chords from tags. Dashed gold on the Galaxy = groups you named.

## Local verify
- `metadata.owner_verify` — never publish (`stripOwnerLocalForPublish` negative test).
- Persist via `ownerVerifyPersistPatch` — do **not** write public `verification`.
- Trust action refuses without `ownerVerified`.
- Nobody else sees this flag.

## Grow
Header **Grow** (Ceremony tab removed). QR + short link; uses 1–7 (multi-use not live on relay). Mycelial: their friends joining them does not add strangers to your book.

## Galaxy
Tab label. Nameplate: fingerprint for compare-aloud + in-person / another-channel verify, then Trust.
Inbound Distress paints a vivre: ember in the star, burning corner on the card. **I went** clears the mark on this device only. Sender Recovery → Distress is silent (no sent receipt).

## Bottom
`TRUST_RECIPE_COPY.manifesto*` — SVRNTY / never lose it / never give it away / post-quantum · local-first · social-recovery / you are not a product. Social-recovery is the stance (Shamir is real); rebuild-from-circle is not a click yet.

## Fleet ask (the living wire)

One mailbox path. Three pulses that do not complete today. Receive/poll exists (`consumeInboundContactUpdates`). Send is still a stub. `encryptContactUpdateTo` exists and is not called from Send.

**P0 — the connection is not alive**

1. **Known ↔ mutually Known.** Grow/join is still a one-way dead-drop. Both books must get the card (join-ack). Until then the connection is not alive.
2. **Trust ↔ mutually Trusted.** Local Trust is a one-way mark (`stub-not-live`). Need deposit + consume so `they_trust_me` / `reciprocal` flips. Same for break.
3. **Contact methods after mutual Trust.** Plug CUR-1 Send e2e: allowlisted delta → `encryptContactUpdateTo` → mailbox deposit. After mutual Trust, a new email/phone/handle must actually arrive. Signal/URL still need allowlist grow. Restore-previous is `signing-not-live`.

**Recovery**

4. **Update Guardians** — select / rotate holders. Shamir split is real; `recoverFromShards` / collect-back is not a click (CUR-9).
5. **Update password** — re-wrap this device’s unlock. Not a website login.
6. **Update seed / recovery code** — new root; old one is done; re-seal the recovery vault.
7. **Distress / key-compromise / Guardian summon** — same envelope class. Outer layer is routing (mailbox key; relay unwraps outer, forwards to recipient mailboxes). Inner is opaque. Sender’s phone keeps no proof, no local history, no toast. Relay stores no Distress state. Recipient’s Galaxy shows an ember in that star; the card burns until they act in the world. Copy: check on them IRL; approach with caution. Default: all Guardians. Optional: one.

**Transport**

8. **WebSockets / subscribe** — replace 5s poll as the live consume path.
9. **Push** — wake the phone for Distress / summon. Payload still opaque.

**Also blocked on fleet (not P0, still real)**

- Grow uses 1–7 is glass intent; relay is still single-use. Need multi-use counters + kill unused slots.
- PSI / `visible()` know-layer and trust-layer overlays. Glass will not invent chords from tags.
- Hybrid-PQ message envelope (signatures are live; encryption is still classical).
- Block stays local (relay stays blind) unless fleet wants a mute that is still not a server ban.

