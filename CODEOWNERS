# svrnty CODEOWNERS — crypto / key / vault-format surfaces require review before merge.
#
# ENFORCEMENT: this file only auto-REQUESTS review until branch protection on `main`
# has "Require review from Code Owners" enabled. Turn that on to make the freeze binding
# (recommended for any unsupervised window — a crypto/format/key change must WAIT for
# review, never auto-merge while the fleet is away).
#
# Rationale + the full render-glass boundary: .cursor/rules/svrnty-gap-freeze.mdc

/src/lib/crypto/          @palberts22
/src/lib/sync/vault.ts    @palberts22
/src/lib/identity/        @palberts22
